/**
 * @param {number} n
 * @return {number[]}
 */
var printNumbers = function (n) {
    let result = []
    for (let i = 1; max = 10 ** n, i < max; i++) {
        result.push(i)
    }
    return result
};

console.log(printNumbers(1))